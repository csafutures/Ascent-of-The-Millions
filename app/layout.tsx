import { Cabin, B612_Mono } from 'next/font/google'
import "/public/assets/css/bootstrap.css"
import "/public/assets/css/widgets.css"
import "/public/assets/css/color-default.css"
import "/public/assets/css/fontello.css"
import "/public/assets/css/style.css"
import "/public/assets/css/responsive.css"
import "./globals.css"
import type { Metadata } from "next"

const cabin = Cabin({
	subsets: ['latin'],
	display: 'swap',
})

const b612Mono = B612_Mono({
	subsets: ['latin'],
	display: 'swap',
	weight: ['400', '700'],
	variable: '--font-b612-mono',
})

export const metadata: Metadata = {
	title: "The Ascent of The Millions",
	description: "Generated by The Ascent of The Millions"
}

export default function RootLayout({
	children,
}: Readonly<{
	children: React.ReactNode
}>) {
	return (
		<html lang="en" className={`${cabin.className} ${b612Mono.variable}`}>
			<body className="home">{children}</body>
		</html>
	)
}
